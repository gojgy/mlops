# Разбор дефектов hw4-broken

Данные — свой датасет из ДЗ 3 (arXiv «аннотация → заголовок», train 45 772 /
val 5 736), токенизатор Qwen3-1.7B. «Было» — исходный код `init hw4` на тех же данных.

## 1. В лосс попадает промпт

**Был.** `mask_prompt()` возвращала `input_ids` как есть, граница промпта — заглушка `n_prompt = 0`.

**Проявлялся.** Под `labels != -100` декодировался весь диалог с `<|im_start|>system`:
модель училась бы повторять инструкцию и аннотацию, а лосс выглядел бы лучше — промпт
предсказать легче, чем заголовок.

**Исправлен.** Промпт строится тем же `build_chat_text(…, add_generation_prompt=True)`,
что на инференсе; граница — `prompt_token_len()` по токенам; на первых `n_prompt`
позициях `labels = −100`.

**Число.** В лосс шло 8 571 711 из 8 571 711 токенов train (100%). Стало **702 900
из 9 797 016 (7,2%)**; проверка 1 декодирует маску у всех 45 575 примеров и получает
ровно `messages[-1].content`. Запасной путь BPE не сработал ни разу.

## 2. Шаблон обучения собран руками

**Был.** Путь обучения склеивал реплики строкой `TURN = "<|im_start|>{role}\n…"`,
путь инференса шёл через `apply_chat_template`.

**Проявлялся.** С `enable_thinking: false` шаблон Qwen3 вставляет в промпт пустой
`<think>\n\n</think>\n\n`; ручная сборка о нём не знала. Строки расходились на **байте
1002** первого диалога val: инференс `…assistant\n<think>…`, обучение `…assistant\nGeometry…`.

**Исправлен.** Оба пути — один вызов `apply_chat_template`, различие только
в `add_generation_prompt`; `TURN` удалён.

**Число.** Инференс — побайтовый префикс обучения на всех **5 736** диалогах val
(было расхождение на первом же). Пустой `<think>`-блок попадает в промпт
и маскируется — под маской только заголовок (проверка 1).

## 3. Паддинг справа

**Был.** `tokenize.padding_side: left` из `params.yaml` не передавался токенизатору,
и в метрики уезжал его дефолт `right`; у `DynamicPaddingCollator` дефолт тоже `right`.

**Проявлялся.** В батче из примеров длиной 95 и 484 короткий получал 389 `<|endoftext|>`
**после** промпта: перед первым сгенерированным токеном модель видела `<|endoftext|>` × 6,
отсюда мусор в начале ответа при `batch > 1`. При `batch = 1` паддинга нет.

**Исправлен.** `tokenizer.padding_side` берётся из params в `main()`, дефолт
коллатора — `left`.

**Число.** Те же шесть позиций после починки — ` rarefaction by boundaries<|im_end|>\n`,
конец промпта. `left` в params, метриках, `.pt` и коллаторе (проверка 4).
Паддинг до `max_seq_len` на batch 8 давал бы 56,6% pad-токенов, динамический — 37,6%.

## 4. Обрезка не считается, max_seq_len не тот

**Был.** `truncation_stats()` возвращала `{}`; `max_seq_len: 232` — под курсовой датасет.

**Проявлялся.** Отчёт писал «обрезано: НЕ СЧИТАЛАСЬ». На нашем распределении при 232
обрезалось **35,0%** примеров, у **13 457 (29,4%)** промпт длиннее 232 и ответ
вырезался целиком; из-за дефекта 1 такие примеры не отбрасывались.

**Исправлен в два приёма.** (а) `truncation_stats()` считает число и долю обрезанных,
предупреждает выше `truncated_warn_ratio` — проверка 5 стала честно красной: 35% при
пороге 5%. (б) `max_seq_len: 512` по хвосту: p99 длины train — 481 токен.

**Число.** Обрезано train **255 (0,56%)**, val 0,33%; 197 примеров без ответа
выброшены и посчитаны. Альтернативы: 384 → 4,65%, 448 → 1,65%, 640 → 0,07%
ценой +56% длины батча ради 0,5% примеров.

## Сверх дефектов

- `model.name` → `Qwen/Qwen3-1.7B`, `tokens_per_sec` → 23 — модель и замер из ДЗ 1.
  Словарь и шаблон у 0.6B и 1.7B одинаковые, статистика не меняется.
- Прогноз обучения: 9,8 млн токенов × 3 эпохи / 23 ток/с ≈ 355 ч. Для ДЗ 5 —
  учить на срезе (v1, 16 128 примеров) и с packing: 5 697 → 2 459 шагов (−57%).
- `make check` выставляет `HF_HUB_OFFLINE=1`: токенизатор в кеше, а онлайн-запрос
  в HF Hub при плохой сети висел до таймаута и валил проверку 2.
- `tests/check.sh` не менялся, отпечаток `f2002c8c9519`.
